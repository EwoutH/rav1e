os: osx

addons:
  artifacts: true

install:
 - brew update
 - brew create https://github.com/xiph/rav1e/archive/20190129.tar.gz
 - brew audit --new-formula rav1e

script:
 - brew install rav1e
 - brew install --debug --verbose rav1e
